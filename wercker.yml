box: node

build:
  steps:
    - npm-install
    - script:
      name: build
      code: npm run build

deploy:
  steps:
    - git-push:
      gh_oauth: $GITHUB_TOKEN
      gh_pages: true
      basedir: dist
      repo: $REPO
  after-steps:
    - slack-notifier:
      url: $SLACK_WEBHOOK_URL
      channel: website
      username: bot
